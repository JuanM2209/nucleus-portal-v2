[workspace]
members = ["nucleus-agent", "nucleus-common"]
resolver = "2"

[workspace.package]
version = "0.25.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
strip = true
codegen-units = 1
panic = "abort"
